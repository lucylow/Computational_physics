def cartesian(r,theta):
	x = r*cos(theta)
	y = r*sin(theta)
	position = [x,y] # list with 2 elements
	return position 

# Note other iterations of the last line work too...
return[x,y] 
return array([x,y], float)
return x,y 

a,b = catesian(r,theta) #this works too 
